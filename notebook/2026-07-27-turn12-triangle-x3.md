# 2026-07-27 · turn 12 — the triangle lemma, (CC+), and X ≥ 3: the X = 2 layer never existed

## 0. Scope

PLAN attack #1: the m = 22 → 23 campaign over the 9,224 X = 2 survivors —
stratify them, per-edge (CC) with real b-profiles, the λ-rigidity of X = 2.
The turn ended somewhere else: the 9,224-configuration frontier field does
not exist. Certificate **0016**: **X ≥ 3 for every critical core at m = 22.**

## 1. An error of mine, first

My desk sketch of the per-edge caps used **Φ(8,5) = 4**. It is **3**
(2+2+2+1+1), so plain (CC) permits Δ = 9 at X = 2 — 6,099 of the 9,224
survivors carry a degree-9 vertex. Caught by the measurement lane in
phase 1. The D-017 shape again: a guessed constant that flattered the
desired conclusion (Δ ≤ 8). The correct Δ ≤ 8 at X = 2 is real, but it needs
the *sharpened* budget below, not the miscomputed Φ.

## 2. The mechanism: a = 2 is a luxury the excess cannot afford

0015's corner a·b ≤ (3/2)(s−1) is tight exactly at (a,b,s) = (2,3,5), and
a = |f∩g∩e| = 2 means three distinct pairs {f,g}, {e,f}, {e,g} each carry
λ ≥ 2 — **the triangle lemma (T): a ≥ 2 forces X ≥ 3** (exhausted over all
5⁶ agreement patterns in the cert; minimum exactly 3; 636 a = 1 teeth). At
X ≤ 2 the corner collapses to **a·b ≤ s − 1** and

**(CC+):  Σᵢ Φ(dᵢ−1, 5−bᵢ) ≤ I_e ≤ X − x_e**  (ledger: 0013 + 0015 steps 2–3)

— the factor 3/2 gone. Per-edge Φ-budgets at X = 2 become 2/1/0 by x_e;
complete heavy patterns {}, {7}, {7,7}, {8} / {}, {7} / {}; Δ ≤ 8 (now a
theorem, and *not* load-bearing: W ≤ 24 implies it). The pair-sum form:
Σ_e I_e ≤ Σ_{excessive pairs}(λ−1)·Σ_{u∈f∩g}(deg u − 2), and the shared
vertices sit on the excess edges themselves, whose budgets cap their
degrees. λ-trichotomy of X = 2 → **W ≤ 24 / 16 / 18** by case, so W ≤ 24,
where W = Σ_v deg·Φ(deg−1,5). The field: (D2) leaves 17,969 of 210,713
with min W = **27 > 24**. Zero survive. With 0015's X ≥ 2: **X ≥ 3**.

## 3. The margin is in the (D2) coordinate — one unit

The phase-2 refuter's catch, and the turn's second D-017 lesson: the
comfortable "4 units of W" (28 vs 24) is the wrong coordinate. W ≤ 24 alone
leaves 843 configurations; **min D2 among them is 12 — exactly one above
0008's cap**. Relax 2·D2 ≤ 22 to 24 and nine survive (near-miss printed in
the cert: Δ = 8, D2 = 12, W = 23). All three W-minimizers sit ON the D2
boundary. 0008's m = 21 knife edge has reappeared one rung up.

## 4. Adversarial record (three fleet phases, all Opus lanes; desk re-derived everything consumed)

- Phase 1 (stratify + 2 derivation lanes): the Φ(8,5) catch; lane A found
  (T)/(CC+) and the kill; lane B proved the cover side (below).
- Phase 2 (refuter + blind re-deriver + reimplementation ×3 code paths):
  zero fatal. (T) exhausted (5,384/5,216/168 distinctness split carried
  honestly, not smoothed); every field constant reproduced ≥ 2×; the margin
  coordinate repaired; Δ ≤ 8 redundancy found; 0015 label drift found.
- Phase 3 (draft to binding desk spec + hostile audit): 20 sabotages, 17
  red, 3 inert-by-theorem. Substantive catch: the draft enacted the
  pigeonhole against 5 − bᵢ classes while its covers had size < 5 — a
  systematically *weakened* test dressed as (CC+). Repaired by padding
  covers to exactly five cells; re-pinned (175 tight / 5,980 / 56,584 /
  54,119); the honest form still shows zero failures.

## 5. Banked from lane B (proven by the fleet, NOT consumed by 0016)

B1: Σ_{u∈T_e} deg u = (m−1) + c_e (desk-verified, trivial). B3: c_e ≤ 7/4/2
at x_e = 0/1/2 via the 25-cell count (fleet-proven, not yet desk-re-derived).
B4: the cover-degree window kills both sides. **B5: PLAN rung (b) CLOSES
NEGATIVE** — min-over-b of Σ Φ(dᵢ−1, 5−bᵢ) equals the Φ(·,5) relaxation
whenever Σ(6−dᵢ)⁺ ≥ 5, which (L4) forces at m = 22 for all X ≤ 4; first
bites at Σdᵢ = 32. B6 (CC*): the column dual — cover-vertex degrees capped
(x_e = 2 → all ≤ 5). B7: joint per-part bound (0 marginal kills). Spin-off
for the ceiling lane, **fleet-claimed, unverified at the desk**: Φ(n,k) ≥
n−k inside (CC) gives X ≥ 2m(m−26)/(3m−10) — X ≥ 289 at m = 456. Verify
before consuming.

## 6. Errata

- Turn-11's "the 9,224 X = 2 survivors are the m = 23 campaign's field"
  (notebook §6 and the then-PLAN) is superseded: that layer is empty. The
  turn-11 entry stands as written (append-only); PLAN rewritten this turn.
- 0015's check-18 label "the floor lands at exactly X ≥ 2, not higher"
  overclaimed (judge-fact stated as floor-fact). Reworded same commit;
  erratum in 0015's NOTES; check condition untouched; re-verified green ×2.

## 7. What opens

The real frontier is the **X = 3 layer: 186,086 configurations, 15,340
alive** under everything available there. At X = 3, a = 2 is legal (the
codegree-3 triangle consumes exactly X = 3), (CC+) is unavailable, and 24
does not transfer. Natural next lemma: the a = 2 world at X = 3 is exactly
the codegree-3 triangle (rigidity). The L1 degree-array judge is written
down and waiting. Sixteen certs green ×2.
