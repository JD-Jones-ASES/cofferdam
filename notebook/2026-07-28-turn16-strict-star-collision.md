# Turn 16 — strict star-collision: X ≥ 6 everywhere, the staircase, the m(m−25)/38 law — and a retraction owed

2026-07-28, third turn of the day. The third Sol Pro review arrived as a file
(retained verbatim: `raw/2026-07-28-received-turn16-gpt-pro-ryser-2.md` — the
first under D-038's new retention clause) carrying: a correction of OUR
turn-15 record, four new lemmas, two theorems, and a quadratic law.

## The retraction (D-038)

The review's §1 is right: the turn-15 record's "circular flooring step" charge
against the second review was FALSE. Its w := Φ(d−1,5) is an integer at every
d, so ⌊16/3⌋ = 5 needed no cap. The circle existed only in the desk's
refuter-lane transcription (w restated as a table + an inequality above
d = 11); the refuters were right about their input; the desk misattributed.
Deeper (turn-16 lane): even in the transcription the step was
unjustified-but-INERT. Errata now run inline through every turn-15 document;
D-038 is law (verbatim routing + retention + re-check against the original);
the turn-15 received text is retained as a marked reconstruction.

## The new lemmas (0020, all derived in-cert, all blind-proved twice)

**(q3)** λ(e,f) = 5 makes e∩f a 5-cover (e\S, f\S sit in one part; no edge
avoids S) — so λ ≤ 4, q ≤ 3, R ≤ 4X. **(BDH)** the 0019 fibre count kept
whole: F(d(z)) = Φ(d(z),5) ≤ s(z). **(SSC+)** pick the q_max pair through z,
let f avoid its shared set T (needs τ ≥ q_max + 2 — automatic at τ = 6, and
INDEPENDENT of (q3): |T| ≤ 5 by distinctness): the pair splits classes, so
F(d(z)) + q_max(z) ≤ s(z). **(SG)** summed: P + H ≤ R. **Key cap** (drives
everything): F(d(v)) ≤ X − q₁ for EVERY vertex — in the q₁-set subtract
q_max ≥ q₁; outside it s(v) ≤ X − q₁. **(Q)** the residue identity
5d + 10F(d) − d² = r(5−r) summed against both moments: 10P = m² − 25m + 2X + Σ₅,
so m(m−25) + Σ₅ + 10H ≤ 38X. **(H1)** H = 0 forces P = 0, impossible at
m ≥ 26 — so at 456: X ≥ 5173 (the +1 past the review's 5172 is ours).

## The engines

§3: X = 5 over all partitions (parts ≤ 5; the wide rows die by census, so
(q3) is billed ONLY to (Q)) × m = 22..26 under the key cap + Δ ≤ 9 (SSC:
F + 1 ≤ 5) + moments + n ≥ 36 + (D2): 35 cells, all dead, margins ≥ 2 —
plus m = 27..31 as belt over 0019 T-B. **X ≥ 6 everywhere.**
§4: X = 6..9 sweeps to the C3 ceilings, per-partition key caps + the
un-weakened (SG) budget Σ(F + q_min): **92 cells, one arithmetic survivor**
(8,30,(3,3,1,1)) — dead structurally: |S_i ∩ S_j| ≤ 1, every high vertex in
≥ 2 shared sets (else s = q_max forces F ≤ 0), each index pair hosts ≤ 1, so
max Σ(d−2)(d−5) = 40 and Σd² ≤ 940 < 1066. **The staircase**: X = 6/7/8/9 ⟹
m ≤ 26/28/29/31. (D2)-free by construction; measured identical with it on.

## The review's one real defect — and the fleet record

Its §7.2 named ONE surviving partition at (8,30); the arithmetic leaves
THREE. All three verbatim-file refuters found it independently; three
independent repairs converged on the T_v profile maximizer; M10 proves the
repair's key constraint load-bearing (drop |T_v| ≥ 2: 1090 > 1066, kill
fails). Its §7.1 X = 6 row also mixed cap conventions (per-partition scans
close everything). Its §6.2 table is CORRECT under its own model — the build
briefly called it "withdrawn" and the cert audit caught that as a fresh
deflation; the shipped NOTES carries the two-model comparison instead.
Fleet: 9 verification lanes (both lemma lanes PROVED all six statements;
1.3M+ family enactments; the τ-threshold τ ≥ q_max + 2 located exactly, with
τ = q_max + 1 witnesses at q_max = 1,2,3,4), then drafter + 3 auditors + fix
lane. Cert audits also caught: a vacuous (SG) enactment (fixed with a
purpose-built witness family — P + H = 3 ≤ R = 30, the corpus's first
non-vacuous reading), the (H1) attribution, and stale cross-references.

## Banked, not consumed

The m ≤ 25 by-product at X = 6 (measured: m = 26 dies via the maximizer,
822 vs 818 — promote next turn after an independent derivation) · the
review's two-tier cap model (correct, weaker) · the ceiling's softness:
(Q) at X = 5172 self-contradicts (forces all degrees ≤ 5, Σd² ≤ 13,680 vs
220,560 required) — the true ceiling floor is far above 5173.

## State after turn 16

**X ≥ 6 on every rung; X ≥ 7/8/9/10 from m = 27/29/30/32; X ≥ ⌈m(m−25)/38⌉
window-wide; X ≥ 5173 at the ceiling.** Nineteen green certificates. The
frontier: X = 6 on m ∈ {22..26}.
