# Turn 19 — 2026-08-03 — cert 0023: the X = 7 layer is empty

## The turn's shape

No peer text this turn — in-house desk work on 0022's open wall, with four
adversarial Opus lanes on the desk's derivation (D-036's fleet discipline
applied to OURSELVES: the desk's proof went to hostile refuters exactly as
a peer's would).

## The wall fell at the desk

0022 left (7,24) as "a wall counting alone cannot breach" — the triangle
branch of (2,2,2,1) ties Λ₇(24) = 62 exactly. Working the tie showed
EVERYTHING at it is forced: unique optimizer state (t = 3, S₄ through the
top cell, degrees (10,9,6) = T), unique census (12,5,0,16), n₂ = 12
maximal → **every edge carries exactly one degree-2 cell** ((D2) per-edge,
on the core). Three independent gates then close it:

1. **Pigeonhole (refuter lane's shortening, adopted as primary):** every
   edge must reach 29 (S5); a high-free edge maxes at 2 + 25 = 27; high
   incidences 25 − 9 (triangle) = 16 < 21 remaining edges.
2. **The apex dies by (S5) arithmetic** (two {3,5} cells summing 7).
3. **(P2) parity:** plain edges through the degree-10 cell need four
   {3,5} cells summing 17 — four odds sum even. (The degree-9 cell yields
   no parity — stated in-cert so nobody misreads the non-contradiction.)

## Then the whole layer

The remaining shapes at m ∈ {22,23,24} fell to: rebuilt m-independent
walls ((3,2,2) redone **c = 5-clean** — at these rungs a q=3 pair may
share an edge with a q=2 pair, so 0022's edge-disjointness opening is
void; the union bound never needed it) · the s-hosting/μ-triangular
enumerations ((2,2,2,1) nontriangle: every reaching multiset forces both
top cells into every support edge → C(4,2) > 4; (2,2,1,1,1): the
C(r,2) = 5 and μ = 2 walls) · and the new **(LG) incidence ledger**: the
forced K4 configurations of (2,1⁵)/(1⁷) hand every census row a small
exhaustive feasibility instance over per-edge degree profiles (a degree-d
cell lies in d edges; totals must hit 2n₂/3n₃/4n₄/5n₅ exactly). All
infeasible: at 22 the x=3 support profile does not exist (sum 10 < 11);
at 23 the 2-count strands what the 3-count spares; at 24 the 3/4-counts
exhaust jointly. The (1⁷) seventh pair runs ALL SEVEN placement postures
— the desk's own first draft had two, caught while wiring the solver.

**Cert 0023, 39 checks, green ×2: X ≥ 8 EVERYWHERE. Staircase: X ≥ 8 on
[22,28] · X ≥ 9 at 29 · X ≥ 10 from 30. Frontier: X = 8 on [22,28].**

## Fleet reconciliation

- **Parity refuter:** every desk step CONFIRMED; found the pigeonhole
  shortening (adopted as primary); named the tie's single pillar ((SJ)
  slack, RG alone holds 62 — without RG the branch reaches 83) and the
  shared-fragility fact (gap ≥ 3 breaks all gates at once). In-cert as
  CK_PILLAR. Census-emptiness measured to have ZERO bite at m = 24 —
  the m = 26 tool retired at this rung, recorded in-cert.
- **Audit simulator:** caught the draft's missing (1⁷)-(10,8,8) family
  (now §5b — it beat the ledger at m = 22 and fell to RG, recorded);
  caught the front-page 5172/5173 drift (the bare law's ceiling is 5172;
  (H1) owns the unit — fixed in README + 0020 NOTES, dated); caught
  three 0022 prose defects (erratum filed). Every numeric probe it ran:
  CONFIRMED.
- **Profile refuter:** shipped cert CONFIRMED sound; three defects in the
  desk's informal narrative are sidestepped by the ledger's incidence
  counting (built for exactly that); one cert prose overclaim repaired
  (the (2,1⁵)-(10,9) J = 7 sub-branch, now enumerated, same
  non-triangular kill). **And it found (PC)** — see below.
- **Blind sweep (post-ship, reconciled):** every table matches the cert
  exactly. Its two survivor claims adjudicated at the desk: the (2,1⁵)
  rows are support-stage verdicts (full ledger + profile refuter close
  them); the (1⁷)-(10,10)-k=3 reopening is refuted by pair-budget
  (2 × s ≥ 6 = 12 > 7 without a shared link — which IS the K4; its k=3
  second cell caps at d ≤ 8, the §5b family). Its contributions, banked:
  **(RG2)** the supply/demand sieve (Σ C(k_ab,2) = Σ C(q+1,2), demand
  k_ab ≥ d(a)+d(b)−m+7 — desk-verified, reproduces 0021's X=6 table) and
  the parity scope law (bites exactly on zero-margin rows). The lane's
  own caveat list is a model of honest reporting.

## (PC) — the turn's biggest find, banked

Σ_{j≠part(u)} Φ(d(u), n_j) ≤ X per cell u (three lines: s(u) is EXACTLY
the per-part agreement count; each part is a box-partition; Φ is the
pigeonhole floor; s(u) ≤ X trivially). At n = 36: **X ≤ 9 caps every
degree at 7** — the entire high-degree case space of 0021–0023 is
retro-vacuous, and the X = 8 campaign starts 80% done (lane-swept:
m = 27/28 die outright, 5 of 22 partitions survive at m ≤ 26). Found by
the profile-refuter lane; desk-re-derived same session; **banked per
Certificate Law** — 0023 does not consume it; cert 0024 opens with it.
Why the house never saw it: 0019/0020's F(d) = Φ(d,5) is the same idea
computed against five ANONYMOUS fibres; using the parts THEMSELVES as
boxes with their true sizes (n_j = 6 at n = 36) multiplies the floor
five-fold at high degree. The lesson for the ledger of lessons: when a
bound comes from a partition, ask what the partition's ACTUAL box sizes
are before reaching for the generic floor.

## New instruments, named

**(P2)** the parity sieve · **(LG)** the incidence ledger · the
pigeonhole-on-high-incidences pattern · **(PC)** part-collision (banked).
The 6-partite tiling test measured again: the (7,24) template TILES —
still banked, still unconsumed, now twice-honest.
