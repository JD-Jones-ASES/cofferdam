# Turn 19 — pre-audit: what the sixth review will probe, and where we stand

*Desk predictions written BEFORE the audit arrives, so the next intake can
be scored against them. A sixth-audit simulation lane (Opus, hostile) ran
against the live repo; its findings are folded in below and marked [SIM].*

## Predicted probes on 0022 (shipped turn 18)

1. **The zero-margin cells.** (1⁷)@26 at Ψ = Λ = 73 (census + capacity,
   both enacted) and the three 70-vs-68 equality forcings at 25. Expect
   recomputation; both gates are exhaustive enumerations — should hold.
2. **The o=2 apex story.** The audit's own repair was shown incomplete
   here; expect it to re-derive the C3 rescue and check our witness's
   x_e = 5 statement (CK_APEXC3 pre-answers it). Watch for a *frame*
   charge: "the witness violates C3 at your rungs" — answered in-cert:
   the witness is for the structural pattern, C3-dead at 25/26, live at 24.
3. **The census solver.** Its row enumeration bounds (gap//15 ladder,
   n₂ ≤ ⌊m/2⌋) — an auditor may test the n₂ cap's necessity. M-CEN in
   0023 now prices exactly that.
4. **M-RG sensitivity** — 0022 states the (7,25) triangle kill reopens
   under RG 5→4. Expect the audit to verify RG itself (τ(K_U) ≥ 6−k via
   the pairing cover) rather than take it.

## Predicted probes on 0023 (this turn)

1. **(P2)'s scope.** The parity sieve is valid ONLY where the census
   forces n₂ = m/2 with m even and n₄ = 0. The cert states the gates
   explicitly per use; the predicted charge is a scope-inflation reading.
   The wording in §4 was written against that reading.
2. **The ledger inventories.** The (1⁷) seventh-pair posture list (seven
   postures) is the likeliest target — the desk itself caught a
   two-posture draft being incomplete. The posture derivation is now a
   comment block + the class arithmetic; an auditor should try to
   construct an eighth posture. Pre-answer: S/S is excluded by q ≥ 2 on
   shared T; A/A puts a in the seventh set (|U|=3); a,b cannot co-ride a
   non-support edge (two shared cells with every support).
3. **"Ordinary cells are low."** Used to bound profiles — rests on the
   budget equalities (P = 10 = R − J for (2,1⁵); P ≤ 11, J ≥ 3 for
   (1⁷)). A third high cell adds F ≥ 1 and breaks them. Stated in-cert;
   expect verification, not refutation.
4. **The (3,2,2) c = 5 rework** — the audit will check that no c = 4
   habit survived (its own turn-18 note flagged this exact hazard for
   m ≤ 24). §1's check 5 spells the c = 5 permissions out.
5. **(2,2,2,1) nontriangle |U| = 5 forcing at J = 11** and the b-split
   enumeration — the b-outside variant is enumerated (bU = 1); an auditor
   may look for bU = 0 (S₄ fully outside U): then J ≥ 12 and the budget
   dies numerically (check's J ≥ 12 line). Pre-answered.

## Predicted next-theorem proposal

The audit will likely propose the X = 8 campaign's opening: partitions of
8 at the new frontier [22, 28], c₃ = ⌊(76−2m)/5⌋ = 6 at m = 22..25, 5 at
26..28 — the q₁ = 3 world reopens at low m (3+3 = 6 ≤ 6: q=3 pairs may
share an edge at m ≤ 25!). Expect: raw sieves against Λ₈(m) = Λ₇(m) + 2,
the same wall/ledger machinery, and new case work where c = 6 voids the
edge-disjointness arguments. The desk's own next steps (PLAN item 1) are
written to match, so the two programs can be compared honestly on arrival.

## Standing exposure, named

- (P2) has **no backup gate** at (7,24) — if a flaw is found in the
  exactly-one-2 forcing, the rung reopens (M-P2 says so in-cert).
- The ledger option lists are generated, not hand-written (M-OPT), but
  the CLASS inventories are hand-derived per posture — that derivation is
  the file's most human step and the most likely site of a real find.
- 0021's banked bonus rungs and the adjacency-aware J-bound remain
  unaudited bank items; an audit may press for their promotion or
  retirement.

## [SIM] The simulation lane's findings — and the score

The hostile sixth-audit simulation (Opus, reading the shipped turn-18 repo)
delivered before 0023 shipped. What it found, all desk-verified and repaired
pre-ship:

1. **A real completeness hole in the 0023 DRAFT** (via its next-theorem
   sketch): the (1⁷) reaching list at cap 6 includes (10,8,8) hosting on
   μ = {3,3,1} — a live family the draft never enumerated. Repaired: the
   full reaching lists are now generated and host-tested in-cert; the new
   double-triple family died only after real work (RG at 22 — found when
   the ledger honestly returned FEASIBLE — ledgers at 23/24).
2. **T1, front-page false arithmetic**: README credited the bare quadratic
   law with 5173 at 456; the bare law gives 5172 exactly, (H1) supplies
   the unit — the very unit 0020's M8 exists to price. Fixed in README and
   0020 NOTES (dated correction).
3. **0022 P4/W3**: the (0,2) same-shared-set sub-case was asserted, never
   argued — the same stated-not-generated failure mode 0022 charges
   against the fifth audit. Argument added; erratum filed.
4. **0022 header counted "Fifteen mutants"** over a file pricing ten.
   Fixed; erratum.
5. **W1/W2 overstatements** ("load-bearing for the tie" — the o=2 branch's
   ceiling is 54 against a 62 wall). Reworded; erratum.
6. **The landscape correction**: the (7,24) "wall" was one zero-margin
   cell among five-plus live shapes; the simulation's per-shape table at
   22/23/24 matches the desk's independently (its "lever they will miss" —
   the profile fact strengthening as m falls — is exactly 0023's §5
   mechanism, found at the desk in parallel).
7. Confirmed across the board otherwise: every probe (P1–P12) recomputed
   and CONFIRMED, including the full triangle optimizer table, both
   census walls at 26, the capacity test, and both green runs.

**Scoring the desk's pre-audit predictions above**: probes 1–4 on 0022
were all exercised by the simulation (as predicted); the (P2)-scope and
ledger-inventory predictions for 0023 were pre-hardened before any audit
saw them — the (1⁷) posture list and reaching lists are now generated, not
stated. The real sixth audit still gets: the (P2) single-pillar exposure
at (7,24) (named in-cert), the m = 22 one-unit ledger margins, and (RG)
itself — 0021's proof of it is the deepest unre-derived input this turn.
