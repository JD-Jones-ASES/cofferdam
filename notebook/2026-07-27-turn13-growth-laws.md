# 2026-07-27 · turn 13 — the excess-growth laws: the window is nonlinear almost everywhere

## 0. Scope and result

PLAN attacks #1(X=3 groundwork) and #2 (the growth spin-off). Certificate
**0017** (61 + 12, green ×2): **(C1) the corner ladder** — a·b ≤ s−1 holds
through **X ≤ 4** (first violator (2,2,4) at forced excess exactly 5;
(4/3)-corner through X ≤ 5; witness where the middle step dies at X = 5,
END-TO-END STATUS AT X = 5 OPEN, flagged loudly) · **(C2) the
unconditional pair-sum** (no a ≤ 1 hypothesis; a COMPLEMENT to (CC), both
directions measured) · **(C3/C4) the linear law** 2m + 5x_e ≤ 52 + 3X per
edge, X ≥ 2m(m−26)/(3m−10) summed, integrality-lifted (X ≥ 3/3/4 at
m = 27/28/29; **290** at m = 456) · **(C5) coupling**: X ≤ 2 ⟹ m ≤ 26;
X ≤ 4 ⟹ m ≤ 28 (C4 alone admits (29,4) — C1 is load-bearing, printed) ·
**(C6) the second-moment law (G)**: Jensen over the 6m incidence slots
with Σ_v deg² = m² + 5m + 2X pinned by the pair identity → **X ≥ 2259 at
m = 456**, crossover with the linear law at m = 67, vacuous m ≤ 31 ·
**(C7) the combined window profile**: forced X ≥ 1 on **431 of 435**
m-values — the only zeros are **m ∈ {23, 24, 25, 26}**, the window's
weakest rungs, where arithmetic forces nothing and the field campaign
must do the work. X ≥ 10 from m = 38; ≥ 100 from m = 108; ≥ 1000 from 309.

## 1. Errors and catches, recorded (D-017 lives here)

- My phase-2 briefing carried a one-index-shifted ψ table (ψ(6) = 1;
  truth Φ(5,5) = 0). A checker coded from it would claim 2323 at m = 456
  — an overclaim in the flattering direction. Caught by the refuter AND
  the blind lane (their models A/B diverged exactly there); the cert
  derives ψ from phi() and carries the shift as a red mutant.
- The X = 5 witness's quoted T_e had cells outside V(K) — repaired
  in-house; and its left side is 0, so it kills the middle step only.
  Labeled exactly so.
- X ≤ 4 ⟹ m ≤ 28 is NOT a contrapositive of the linear law alone
  (refuter's break 2); ledger repaired to C4 + C1.
- The audit's moderate catch: the M2 slack check hard-coded the true
  Σᵢ Φ as 0 on the rehearsal core — now computed.

## 2. Adversarial record

Phase 1 (three lanes): growth-law re-derivation ×2 + τ-generic form
(2m + 5x_e ≤ 3X + (10t−8); Θ = 5σ+1) · (CC4+) found independently twice ·
X=3 shape census (8 classes, complete). Phase 2: refuter (4 breaks, all
spec repairs) + blind numeric re-deriver (every table value ×2, ψ
ambiguity resolved). Phase 3: draft to binding spec + hostile audit
(PASS-WITH-FIXES; 7 sabotages red in-draft + the audit's own 10).
Desk re-derived every consumed step; the b_i = 5 branch adjudicated to
0015 step (1) covering (not lemma (A)) — and the cert EXHIBITS the
branch's 9,015 excluded-state failures rather than asserting safety.

## 3. Banked for turn 14 (fleet-verified, cited not consumed)

X = 3 at m = 22: 15,451 (D2)-passers, field floor W = 27 (ONE config,
single degree-9); star shape dead (bound 24, Claim-B-free); joint bounds
(NOT cert-grade yet — vertex identifications must be enumerated) kill
path/triangle/P3+K2 to that same single config; shape-blind W ≤ 48
leaves 1,580, of which 1,549 are λ4-pair-only — **the λ4 pair (two edges,
x = 3 each, all twelve degrees ≤ 6, row sums exactly 30) is the
frontier**, and it is transversal-level structure the degree field
cannot see. X = 3 impossible for m ≥ 28 (coupling). X = 4: 23 shapes,
max bound 80, field floor 32.

## 4. Repo state

Seventeen certs green ×2. Errata applied same-commit: 0016 §9 note +
docstring ("(CC+) unavailable at X=3" → false as written; the
24-does-not-transfer half stands). README rewritten for the public flip
(AI disclaimer + beta/WIP header, owner-directed). The weakest rungs of
the window are now named in print: m ∈ {23,…,26}.
