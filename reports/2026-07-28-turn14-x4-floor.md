# Turn 14 for JD — the public flip worked: an outside audit handed us a target, and it checked out

**The one-line result: at the bottom of the window (m = 22), the excess is
now at least FOUR — certificate 0018, green twice, eighteen certificates
total.**

## What happened

You made the repo public on turn 13 for exactly this. One day later, GPT 5.6
Sol Pro read it end to end at the current commit and sent back three things:

1. **A clean bill of health** for the certified chain — no mathematical error
   found in any load-bearing step, and it confirmed the old issue-#1 repairs
   are all in the tree (so I closed issue #1 with a pointer to the turn-7
   reply).
2. **Four documentation nits** — two wrong note-tallies, one wrong sentence
   about which certificates are "m = 22 statements", and the stale issue.
   All four were real; all four are fixed with dated errata, prose only,
   both touched certificates re-verified green twice.
3. **A theorem proposal**: the entire X = 3 layer at m = 22 can be killed,
   advancing the floor's excess from X ≥ 3 to X ≥ 4 — with an eight-case
   attack plan and field numbers that matched ours digit for digit.

Under the house rules the proposal came in as *statements only* — I never
read its proofs (the full documents weren't attached anyway). The lab then
did what it does: I derived all eight cases at my desk; nine blind fleet
lanes re-derived them independently from a definitions pack; five hostile
refuters attacked my versions; three more auditors attacked the finished
certificate. Six separate in-house implementations of the field computation
agree on every number.

## The honest part

The refuters earned their tokens twice. I "corrected" one of the audit's
bounds (27 → 26) and **my correction was wrong** — three refuter lanes
independently restored the audit's 27 by brute-force enumeration. And both
my first write-up and the audit's own summary missed one subcase of the
hardest kill; I caught it in self-review, the refuters caught it three more
times, and the blind lanes had covered it without being asked. Both scars
are recorded inside the certificate itself, per the house law that an error
which flatters the expected answer — or deflates a peer's correct one — is
the kind worth remembering.

## What it means

Turn 13 ended with a frontier of 1,549 configurations all pointing at one
structure (the λ4 pair). That entire frontier is now *gone* — the λ4 pair
turns out to be impossible two full rungs below the floor. The new frontier
is the X = 4 layer: 12,171 configurations. The same machinery that just
worked (budgets, pair-sum, shape census) is still valid there, so the next
turn has a clear target, and the thin rungs m ∈ {23..26} remain the
window's weak stretch.

Nothing here changes the publication picture except favorably: the floor
statement is now "m ≥ 22, and any counterexample core at 22 carries excess
≥ 4" — structure on top of the number. Your `awaiting_jd` on publication
strategy stands unchanged.

## The one command

    cd ~/Documents/repos/cofferdam/certificates/0018-x4-floor && python3 verify.py
