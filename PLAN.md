# cofferdam — plan

Revised 2026-08-03 (turn 19). History is the record; this file is rewritten.

## Where we are — **[22, 456]; X ≥ 8 EVERYWHERE; X ≥ 9/10 from m = 29/30; X ≥ 5173 at the ceiling (with (H1); bare law 5172)**

| what | where |
| --- | --- |
| the floor **m ≥ 22**, citing nothing | 0001–0012 |
| the window **m ∈ [22, 456]** | 0013, 0014 |
| per-edge budgets and growth laws — (CC) 3/2 · corner ladder · C3 · (G) | 0015, 0017 |
| the floor-local excess chain X ≥ 2/3/4 at m = 22 | 0015, 0016, 0018 |
| star-collision: X ≥ 5, X ≥ 6 + the quadratic law | 0019, 0020 |
| X ≥ 7 everywhere · staircase 26/28/29 | 0021 |
| X = 7 ⟹ m ≤ 24 (shared-set quotients) | 0022 |
| **THE X = 7 LAYER IS EMPTY — X ≥ 8 everywhere; frontier X = 8 on [22, 28]** | 0023 |

**0023, the live edge, in four lines:**

- **New instruments.** (P2) the parity sieve (at n₂ = m/2 censuses every
  edge carries exactly one degree-2 cell; plain-edge parity then bites) ·
  (LG) the incidence ledger (per-edge low-cell profiles must jointly hit
  2n₂/3n₃/4n₄/5n₅ — a finite feasibility check per census row) · the
  high-incidence pigeonhole (every edge needs a high cell; 16 < 21).
- **The (7,24) tie fell three ways** — pigeonhole (primary), apex-(S5)
  arithmetic, parity — all zero-slack, all standing on residual pairing
  (the (SJ) budget is SLACK there; without (RG) the branch reaches 83).
- **Kills.** Raw sieves ×3 rungs → c=5-clean walls ((3,2,2) rebuilt without
  edge-disjointness; (2,2,1,1,1) μ-triangular; (2,2,2,1) clique forcing) →
  full reaching lists host-tested ((1⁷): seven multisets, three survivors)
  → K4 + double-triple ledgers, every row, every posture — all infeasible;
  the m = 22 double-triple died by RG after the ledger honestly returned
  feasible on two rows.
- **Provenance.** In-house (desk turn 19); adversarially pre-shipped: three
  refuter lanes + a simulated sixth audit, which caught a missing multiset
  family in the draft, two overstatements, and the front-page 5172/5173
  drift — all repaired pre-ship, errata filed (0022, 0020 NOTES).

All green under bare `/usr/bin/python3` 3.9.6 **and** `python3 -O` (D-015).

**The chain, one paragraph.** (A)(B)(C) → ladder → m ≥ 22. Criticality →
Katona → part-confinement → **m ≤ 456**. Excess: (CC) → X ≥ 2 → (T) → X ≥ 3
→ eight shapes → X ≥ 4 → star-collision → X ≥ 5 → strict star-collision →
X ≥ 6 + the quadratic law → the q_max debit → X ≥ 7 → shared-set quotients
→ X ≥ 8 from m = 25 → **profile ledgers + parity → X ≥ 8 EVERYWHERE**;
X ≥ 9/10 from 29/30; X ≥ ⌈m(m−25)/38⌉ window-wide (5172 bare, 5173 with
(H1)).

## Where to attack — reranked after turn 19

1. **CERTIFY (PC), THE PART-COLLISION LAW — then open X = 8 with it.**
   Banked this turn (0023 NOTES §(PC); found by a refuter lane, desk-
   re-derived, three lines): Σ_{j≠part(u)} Φ(d(u), n_j) ≤ X per cell.
   At n = 36 it caps **every degree at 7 for X ≤ 9** — the lane's sweep
   (fleet-claimed, not yet enacted) kills the whole X = 7 layer from
   scratch at ceilings 40–48 vs Λ ≥ 59, re-proves 0021/0022's rungs, and
   at X = 8 kills m = 27/28 outright with 5 of 22 partitions surviving at
   m ≤ 26. Cert 0024: enact (PC) + its mutants (n_j-vector completeness;
   the n > 36 part-size ladder), then the X = 8 sweep on top. c₃ =
   ⌊(76−2m)/5⌋ = 6 at m ≤ 25 (the q₁ = 3 world reopens); Λ₈ = Λ₇ + 2;
   cap X − q₁ reaches 7 where q₁ = 1, admitting d = 11 — except (PC)
   forbids it at n = 36. The sixth audit likely opens here too.
2. **Re-derive (RG) with teeth — and certify (RG2) beside it.** The
   (7,24) kill and both ledger families stand on residual pairing; 0021
   proved it once; (PC) breaches the same cells independently
   (unenacted). The blind lane's **(RG2)** supply/demand sieve —
   Σ C(k_ab, 2) = Σ C(q+1, 2) against k_ab ≥ d(a)+d(b)−m+7 —
   desk-verified, reproduces 0021's X = 6 table, kills every
   two-degree-10 multiset at m = 22 in one line. (PC) + (RG2) together
   are the X = 8 campaign's opening artillery; both need certification
   and mutants first.
3. **Promote the banked bonus rungs** (X = 9 ⟹ m ≤ 28 etc.) — unchanged.
4. **The ceiling is soft** — unchanged ((Q)/(Q0), the J-conjunction).
5. **(EC) quotient completeness as an engine** — the posture lists in 0023
   are hand-derived then exhaustively used; generating them from free
   templates would close the last stated-not-generated seam. Banked: the
   6-partite tiling test (measured twice, never yet the killer).
6. **Per-part hand claim · conditioned-class equality scans** (unchanged).

**Closed levers (do not reopen silently):** turns 10–18 lists, plus: the
X = 7 layer (turn 19: DONE — 0023) · the (7,24) tie (fell to parity/
pigeonhole; do not re-litigate the counting reading, the tie was real) ·
the census-emptiness tool has ZERO bite at m ≤ 24 (measured — every nearby
multiset admits a census; use ledgers there, not censuses).

## Novelty ledger — updated 2026-08-03 (turn 19)

Floor comparator unchanged (Sivashankar m ≥ 14; ours 22) — now with **an
entire excess layer cleared** and the excess floor at X ≥ 8 window-wide.
The two-sided window and the excess profile remain unpublished elsewhere.
Publication is JD's call — `awaiting_jd` stands, five outside-verified
theorems plus 0023 (in-house, adversarially pre-shipped).

## Risk decomposition — updated for turn 19

| step | what stands under it now |
| --- | --- |
| m ≥ 22 / m ≤ 456 | unchanged (0012 / 0013–0014) |
| X ≥ 5/6/7 + staircase (0019–0021) · X = 7 ⟹ m ≤ 24 (0022) | unchanged; 0023 consumes T-A22/T-B22 and 0021's T-B21 |
| **X ≥ 8 everywhere (0023)** | the (7,24) chain: zero slack, three gates, ONE certified pillar ((RG) — M-RG reopens all three rungs, 54/59/62 → 59/62/67; the banked (PC) breaches the same cells independently but is not yet certified); m = 22 ledgers at one unit of profile sum; M-D2E flips the instant kill AND the parity sieve together; the ledger is a relaxation and two of its m = 22 instances were feasible (killed by RG instead — recorded in-cert) |
| (Q)/(Q0)/5172+(H1)→5173 | unchanged (0020); README drift fixed turn 19 |

## Machinery — the working lessons (the law itself lives in DECISIONS.md)

- **Turn the intake law inward.** 0023 is in-house, and shipping it through
  the same hostile lanes a peer text gets caught a missing multiset family,
  a wrong front-page implication, and two overstatements — pre-ship, at
  desk prices. An in-house theorem is a peer text from tomorrow's auditor.
- **A feasible relaxation is a question, not a defeat.** The ledger
  returned FEASIBLE twice at m = 22; the answer was a different gate (RG),
  not a harder push on the same one. Record the feasible instances — they
  are where the next auditor will push.
- **State the null result of a lever** (parity yields nothing at d = 9;
  the tiling test tiles at every template measured) — it saves the auditor
  a false find and the desk a false hope.
- Sweep completeness (M-SWEEP), measurement over expectation (D-035), peer
  intake (D-036): unchanged.

## Standing

- Twenty-two green certificates, each ×2 (bare 3.9.6 and `-O`), plus 0004
  never-green scaffolding. External-input ledger: EMPTY.
- No solver in the trust chain. Fleet outputs entered no chain until
  desk-re-derived (Certificate Law); adversarial records in each NOTES.
- Attribution recorded, not consumed (D-031, D-036): five outside reviews
  credited in provenance; no proof step cites any of them; 0023's
  adversarial lanes credited in its NOTES the same way.
