# cofferdam — plan

Revised 2026-08-03 (turn 20). History is the record; this file is rewritten.

## Where we are — **[22, 456]; X ≥ 10 EVERYWHERE; X ≥ 11 from m = 26; 5173 at the ceiling (with (H1); bare law 5172)**

| what | where |
| --- | --- |
| the floor **m ≥ 22**, citing nothing | 0001–0012 |
| the window **m ∈ [22, 456]** | 0013, 0014 |
| per-edge budgets and growth laws — (CC) 3/2 · corner ladder · (C3) · (G) | 0015, 0017 |
| the floor-local excess chain X ≥ 2/3/4 at m = 22 | 0015, 0016, 0018 |
| star-collision: X ≥ 5, X ≥ 6 + the quadratic law | 0019, 0020 |
| X ≥ 7 everywhere · staircase 26/28/29 | 0021 |
| X = 7 ⟹ m ≤ 24 (quotients) · the X = 7 layer EMPTY (ledgers + parity) | 0022, 0023 |
| **PART-COLLISION: X ≠ 7 independently (second stack); X = 10 ⟹ m ≤ 28; the atlases** | **0024** |
| **SUPPORT POSTURES: X ≥ 10 EVERYWHERE; X = 10 ⟹ m ≤ 25** | **0025** |

**Turn 20, in four lines:**

- **The sixth audit** (GPT 5.6 Sol Pro) proposed the global part-collision
  law + exact sweep + X ≥ 10; intake per D-036 (3 refuters, 2 blind, desk)
  confirmed every table SIX ways (one lane fully blind, with witnesses)
  and refuted three of its steps (attribution; edge-disjointness twice).
- **0024** certifies (PC)/(GPC) above the exact codegree identity and
  re-proves X ≥ 8 on a second disjoint stack (0021→0024 never touches
  0022/0023); the X = 8/9/10 survivor atlases are certified data.
- **0025** kills every atlas cell by GENERATED postures (graphs → closure
  → caps → C3/budget/capacity kills) — the standing cure for the
  three-audits-running posture-gap failure mode.
- **The 0023 erratum was corrected twice**: the audit's own repair
  mechanism was wrong (parity is apex-free — two complete proofs at
  (7,24)); a refuter lane caught the desk's over-retraction in-session.

All 25 certs green under bare `/usr/bin/python3` 3.9.6 **and** `-O` (D-015).

**The chain, one paragraph.** (A)(B)(C) → ladder → m ≥ 22. Criticality →
Katona → part-confinement → **m ≤ 456**. Excess: (CC) → X ≥ 2 → (T) → X ≥ 3
→ eight shapes → X ≥ 4 → star-collision → X ≥ 5 → strict → X ≥ 6 + (Q) →
q_max debit → X ≥ 7 → [two stacks: quotients+ledgers+parity | part-
collision+atlases] → X ≥ 8 → postures → **X ≥ 10 everywhere; X = 10 only
on m ∈ [22, 25]; m ≥ 26 ⟹ X ≥ 11**; X ≥ ⌈m(m−25)/38⌉ window-wide (5172
bare, 5173 with (H1)).

## Where to attack — reranked after turn 20

1. **THE X = 10 CAMPAIGN ON [22, 25].** 0024 certifies its exact target
   list: 74 cells, only n-vectors 6⁶/6⁵7/6⁴7², every shape known. The
   sixth audit's own pointer (its §11) matches the desk read: the m = 25
   survivors hinge on 3-/4-cell shared-set universes — (EC) quotient
   completeness + the posture engine + (RG2) are the assembled artillery.
   Clearing X = 10 moves the frontier to X = 11 ≥ ⌈m(m−25)/38⌉ parity.
2. **Certify (RG2)** (banked turn 19, desk-verified) and re-derive (RG)
   with teeth — the 0023 stack still leans on (RG) at the (7,24) tie;
   0024/0025 lean on neither, so the two-stack redundancy is now real.
3. **Promote (EC) quotient completeness to an engine** — the last
   stated-not-generated seam (0022/0023 posture lists), now with 0025's
   generation pattern as the template.
4. **The soft ceiling** ((Q)/(Q0), the J-conjunction) — unchanged.
5. **Per-part hand claim · conditioned-class equality scans** (unchanged).

**Closed levers (do not reopen silently):** turns 10–19 lists, plus: the
X = 8 and X = 9 layers (turn 20: DONE — 0024+0025) · the X = 10 high rungs
26/27/28 (DONE — 0025) · the union-bound route for q4-adjacent shapes
(REFUTED — the triangles are realizable; use postures) · (C3)/J-budget
on the current atlases (measured BELTS — priced in 0025; the
coincidence law is NOT a belt: three cells reopen without it).

## Novelty ledger — updated 2026-08-03 (turn 20)

Floor comparator unchanged (Sivashankar m ≥ 14; ours 22) — now with
**three entire excess layers cleared in two turns** (X = 7, 8, 9) and the
excess floor at X ≥ 10 window-wide, X ≥ 11 from m = 26. Ryser r ≥ 4
confirmed open per Clow–Haxell–Mohar (arXiv 2505.05339, verified
firsthand). The two-sided window and the excess profile remain
unpublished elsewhere. Publication is JD's call — `awaiting_jd` stands;
six outside audits, six theorem-bearing turns.

## Risk decomposition — updated for turn 20

| step | what stands under it now |
| --- | --- |
| m ≥ 22 / m ≤ 456 | unchanged (0012 / 0013–0014) |
| X ≥ 5/6/7 + staircase (0019–0021) | unchanged; both stacks consume 0021 |
| X ≥ 8: stack A (0022+0023) | the (7,24) tie: TWO complete proofs (pigeonhole; apex-free parity — erratum twice-corrected); (RG) still the single certified pillar there |
| X ≥ 8: stack B (0024) | (PC)/(GPC) + exact DM sweep; (D2)/(KC) are NULLS at the floor and load-bearing only in the X = 10 tail — M-D2R/M-KC price both directions |
| **X ≥ 10 (0024+0025)** | the atlases (six-way verified, one blind, 127 witnesses) + generated postures; spine = derived caps + (PC) + the coincidence law (M-CAPS/M-PCCAP/M-COIN, three flips); C3/J are priced belts |
| (Q)/(Q0)/5172+(H1)→5173 | unchanged (0020) |

## Machinery — the working lessons (the law itself lives in DECISIONS.md)

- **Verify the deflation like the claim — inward too.** The audit
  mis-diagnosed its own correct catch; the desk's first repair imported
  the mis-diagnosis into three files; a refuter lane caught it same-day.
  A proposed repair of our own cert is peer-shaped material: it goes
  through the hostile lanes like everything else (D-036, turn-20 clause).
- **Generate, don't narrate.** Posture gaps survived three audits'
  narration; 0025's graph enumeration with asserted class counts is the
  standing pattern for any finite case space (next: (EC)).
- **Measure belts vs spine — on the FULL atlas.** 0025's M-C3/M-J are
  belts; M-CAPS/M-PCCAP/M-COIN are the spine — and M-COIN was briefly
  misfiled as a belt on a scoped measurement until the full-atlas pass
  landed (three cells reopen). A mutation priced on a subset prices the
  subset.
- Turn the intake law inward · a feasible relaxation is a question ·
  state the null result of a lever · sweep completeness (M-SWEEP) ·
  measurement over expectation (D-035): unchanged.

## Standing

- **Twenty-four green certificates** (0001–0003, 0005–0025; 0004
  never-green scaffolding), each ×2 (bare 3.9.6 and `-O`).
  External-input ledger: EMPTY.
- No solver in the trust chain. Lane outputs entered no chain until
  desk-re-derived (Certificate Law); adversarial records in each NOTES.
- Attribution recorded, not consumed (D-031, D-036): six outside audits
  credited in provenance; no proof step cites any of them.
